services:
  - type: web
    name: stock-ai-api
    env: node
    plan: starter
    buildCommand: npm ci --include=dev && npm run build:web && npm prune --omit=dev
    startCommand: node server.js
    autoDeploy: false
